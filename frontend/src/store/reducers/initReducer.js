
const initialState = {
  init: {},
}

const initReducer = (state = initialState, action) => {
  const { type, payload } = action

  switch (type) {
    default:
      return state
  }
}

export default initReducer
